<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boot.dao.MysqlMypageMapper">
	<!-- 전체 리뷰~ -->
	<select id="getMypageData" parameterType="String" resultType="review">
      select * from review r, member m where r.rmnum = m.mnum and m.mnum=#{rmnum}
	</select>
	<!-- 전체 갯수 -->
	<select id="getMypageListCount" parameterType="int" resultType="int">
      select count(*) from review where rmnum=#{rmnum}
	</select>
	
	<!-- 팔로워 수 -->
	<select id="getfollo" parameterType="int" resultType="int">
 		select count(*) from follower where fmnum=#{mnum}
	</select>
	<!-- 팔로우 명수 -->
    <select id="getFollows" parameterType="int" resultType="int">
        select count(*) from follower where follower=#{mnum}
    </select>
	<!-- 내정보 -->
	<select id="getMyMember" parameterType="int" resultType="Member">
		select * from member where mnum=#{mnum}
	</select>
	<!-- 자세한 내정보 -->
	<select id="getMypageMember" parameterType="int" resultType="Member">
 		select * from member m,follower f where m.mnum=f.fmnum and mnum=#{mnum}
	</select>
	<!-- 내 팔로워들 정보 -->
    <select id="getFollowing" parameterType="int" resultType="int">
        select * from member m,follower f where m.mnum=f.follower and m.mnum=#{mnum}
    </select>



	
</mapper>
